# 实施计划与阶段记录

## M0 契约与方案（已确认）

已建立 PRODUCT_CONTRACT、ARCHITECTURE、EVALUATION 和本文。已检查当前目录：空目录，无现有项目文件；`git status --short --branch` 返回 not a git repository，未初始化、提交或推送 Git。未读取其他项目或凭据。

用户于 2026-09-18 已确认产品契约及无 Cursor 同时写入，授权先做 Fixture。M1 已实现并运行，详见 M1_REPORT.md；模型、素材和预算暂不确定，不在本次执行真实收费调用。

## M1 最小纵向切片（已完成 Fixture 验证）

1. 创建 uv 项目、配置、忽略规则、层次目录与首次 Alembic 迁移；集中中文文案。
2. 关键规则先写失败测试：证据边界、Recipe 编译、能力拒绝、Job 幂等/恢复和上传安全；再完成最小实现并重构。
3. 实现共用上传、独立 Worker、持久化任务、状态轮询、输入/结果对照和下载。
4. 在同一工作台实现 A、独立纸样实验入口、B1/B2、C1/C2；离线 Fixture 输出为可打开图像和结构化资料，页面醒目标记，不当作真实识别。
5. B1 实现透明图/蒙版替代路径和 Pillow 确定性合成；验证编辑参数和处理记录。
6. 运行 Ruff、pytest、迁移与应用/Worker 启动，完成每条路线本地烟测及基本键盘、焦点检查。记录实际命令、结果与本地地址。

退出条件：三个功能每条路线可运行；没有假模型结果；产物可查看/下载；核心安全、证据规则和任务语义通过测试。M1 不声称视觉可行性已验证。

## M2 真实模型可行性

当前进度：国内 Kimi / Seedream 适配代码、逐次外发确认和离线协议测试已完成，39 项测试通过。尚未进行真实调用，M2 可行性退出条件尚未达到。详见 M2_INTEGRATION_REPORT.md。

进入前收齐：理解和图像供应商及精确模型、官方文档/本项目显式配置、用户提供素材、接收方和图片外发许可、币种/总预算/调用次数上限。密钥通过本地环境配置，聊天中不要求粘贴。

先逐项确认实际能力，再实现两个真实适配器；按最少调用验证 A 分析/技术图/实验示意、B1 背景+合成、B2 重绘、C1 与 C2 对照。保存失败与未知结果；不支持则记录阻塞，不能用 Fixture 顶替。预算不足时先明确剩余项目，不自行增加支出。

退出条件：用户可查看真实输出与失败记录；所有未验证/不支持项明确。用户查看后才能进入 M3。

## M3 实用闭环

完善分析与尺寸/工艺编辑、Recipe 保存和跨商品复用、单项修改再生成、候选收藏、人工评价、导出、任务恢复和有界人工重试。早期已有的领域规则与安全逻辑继续复用，避免建立大量管理页。

## M4 求职证据

整理可复现启动说明、真实样例索引、失败样例、C1/C2 实验、技术决策、测试记录、模型限制和演示步骤。区分已实现与已验证；素材默认留本地。未经批准不发布仓库。

## 每阶段汇报格式

- 实际完成内容与工程完成范围。
- 执行命令及真实结果。
- 产物路径与运行入口。
- Fixture 与真实模型的具体状态、视觉质量评价状态。
- 阻塞及未验证项。
- 用户下一步操作。
- 下一阶段所需授权。
