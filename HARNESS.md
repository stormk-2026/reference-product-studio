# 商品视觉工作台：项目 Harness

本文件是本项目实现、测试与运行契约，不是通用 skill。更新日期：2026-09-19。

## 已确认的产品目标

通用电商商品，不限定服装。主要入口是一键混图：自有商品实拍 + 喜欢的网店商品场景 → 自有商品在该场景重新呈现。界面用图片表达角色，不让用户理解内部模式代码。

- 同一商品多角度：首图为主体依据，其余角度补充结构；共同生成一张，仅出现一件商品。不能当成批量商品，也不能拼成多件。
- 多个商品共用场景：每张代表一件商品。先生成第一件，用户不满意可修改意见重新生成；通过后才授权剩余。剩余均携带同一场景图和最终通过的样张统一风格。
- 当前不支持一批中的每件商品再分别拥有多角度分组；不得假装已支持。
- 商品解析保留完整可复制 Prompt 与按图片动态生成的参数两种视图。禁止服装固定属性套用所有商品。
- 白底实物图和 Beta 三视图保留；三视图不作为生产工程图。

## 已验证链路及边界

用户已确认直接 Seedream 场景融合路线通过。不要无故新增必经 Kimi 或抠图步骤。当前一键混图 = 商品原图/同商品多角度 + 目标场景 + 明确角色提示词 → Seedream 一次调用。场景主体判断由 Seedream 在此次生成内完成，并非独立检测器。不能称为像素锁定，也不能宣称模型保证商品完全不变形。

A 与 C_analyze 使用 Kimi；B2、白底图、技术图、C2 使用 Seedream。C2 使用已有视觉配方；B2 不会自动先让 Kimi 分析。可选出图后抠图使用本地 rembg / U²-Net / CPU，输出透明 PNG，无外发费用。不能把它描述成生成前已自动抠图。

## 提示词不可回归的约束

1. 商品图片决定商品身份、轮廓、比例、颜色、材质、部件、Logo 字形/位置/排版及刻度文字；参考场景决定环境。
2. 有目标背景图就以它为场景依据，禁止再添加默认白底颜色建议。2026-09-19 曾因背景参考 + #ffffff 指令冲突生成白底图；原请求确实发送两张图，不得误诊为漏传。
3. 场景已有商品：移除原商品，放入自有商品；没有商品：选择合理承托位置。继承墙地面材质、色彩、布局、光源和投影，不能只模糊地“借鉴风格”。
4. 截图只使用其中有效商品场景，排除手机状态栏、黑边、按钮、价格、原品牌广告和卖点；自有商品本体的文字与 Logo 保留。
5. 不能为仿照参考姿态扭曲商品。融合接触阴影与环境光优先于重设计主体。
6. 多角度必须标明图片角色及编号，只呈现同一件商品；背景和通过样张不能混作商品图。
7. 保存实际 compiled_prompt、图片 ID/哈希、模型、请求 ID、响应使用量和输出原图。结果展示商品、补充角度、目标背景和结果供对照。

## 服务与费用

只用项目显式 .env；禁止输出 key、读取父级凭据或把密钥写入测试/文档。国内 Kimi 与火山方舟白名单；当前精确模型 kimi-k3 和 doubao-seedream-5-0-pro-260628。

真实外发通过预览与绑定请求快照的签名确认；用户在当前会话明确授权重试一次时可按该范围执行。无自动付费重试、无失败后偷偷换模型；批量须首张通过再确认剩余调用次数，幂等防重复收费。不要自行调整账户限额。

Seedream 每次最多10张输入（商品角度、场景、样张合计），一次一张结果；使用2K、b64_json、png。当前 Pro 请求不发送 sequential_image_generation 或 stream；勿未经验证照抄其他版本参数。国内真实联网 Worker 需要网络权限，网络失败不等于 key 错误。

429 必须区分错误码：SetLimitExceeded 是安心体验模式推理限额，余额足也会触发；RateLimitExceeded 系列是频率/吞吐；QuotaExceeded 有多种配额含义；ServerOverloaded 服务繁忙。用户关闭安心体验模式后曾成功返回200。只按安全白名单存错误码，不泄露原始响应中的凭据或URL；不要因 message 出现 model 就称参数错误。早期记录漏存子码，无法倒推。超时未知不能冒充失败未计费。

## UI 与验证

默认一键混图。商品、场景独立上传区，支持文件选择/拖入/粘贴；素材可删。参考图有缩略预览，结果在旁边，历史折叠。生成前区分同一商品多角度与多个商品；不能悄悄把多个商品融为一件。

无需账号登录本地服务。删除素材只隐藏，保留历史追溯。不要删除旧任务、改变用户原图或冒充已通过人工质量检查。

修改后运行 .venv/bin/pytest -q、.venv/bin/ruff check .、node --check src/studio/web/static/app.js，并检查浏览器布局与关键输入语义。测试禁真实网络。付费生成只有明确授权才做。重启前检查 queued/running，不中断活跃生成。默认服务127.0.0.1:8765，独立 studio.worker。

尚待增强：按商品分组的多角度批量、可视化主体位置/占比控制、生成前蒙版人工修正、独立商品保真检查。它们不是已完成功能。

确认预览回归检查：展开“查看发送内容”和勾选授权不能使签名预览失效；仅实际生成字段变化可使预览失效。表单 input 冒泡必须按控件类型、字段白名单与预览区域隔离。开始按钮不可 sticky 遮挡确认区。

2026-09-19 UI 回归根因：body.dataset.tab 用于样式，但通用 [data-tab] 选择器误把 body 绑定成导航按钮，任何点击冒泡都会 setTab → invalidate。导航事件和 active 状态必须限定 .tabs button[data-tab]，禁止使用全局 [data-tab]。
