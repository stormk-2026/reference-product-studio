# 商品视觉工作台：项目 Harness

本文件是本项目实现、测试与运行契约，不是通用 skill。更新日期：2026-09-20。

## 已确认的产品目标

通用电商商品，不限定服装。两条工具入口见下方最新产品契约。混图能力：自有商品实拍 + 喜欢的网店商品场景 → 自有商品在该场景重新呈现。界面用图片表达角色，不让用户理解内部模式代码。

- 同一商品多角度：首图为主体依据，其余角度补充结构；共同生成一张，仅出现一件商品。不能当成批量商品，也不能拼成多件。
- 多个商品共用场景：每张代表一件商品。先生成第一件，用户不满意可修改意见重新生成；通过后才授权剩余。剩余均携带同一场景图和最终通过的样张统一风格。
- 当前不支持一批中的每件商品再分别拥有多角度分组；不得假装已支持。
- 商品解析保留完整可复制 Prompt 与按图片动态生成的参数两种视图。禁止服装固定属性套用所有商品。
- 白底实物图和 Beta 三视图保留；三视图不作为生产工程图。

## 已验证链路及边界

用户已确认直接 Seedream 场景融合路线通过。不要无故新增必经 Kimi 或抠图步骤。当前一键混图 = 商品原图/同商品多角度 + 目标场景 + 明确角色提示词 → Seedream 一次调用。场景主体判断由 Seedream 在此次生成内完成，并非独立检测器。不能称为像素锁定，也不能宣称模型保证商品完全不变形。

A 与 C_analyze 使用 Kimi；B2、白底图、技术图、C2 使用 Seedream。C2 使用已有视觉配方；B2 不会自动先让 Kimi 分析。可选出图后抠图使用本地 rembg / U²-Net / CPU，输出透明 PNG，无外发费用。不能把它描述成生成前已自动抠图。

## 提示词不可回归的约束

1. 商品图片决定商品身份、轮廓、比例、颜色、材质、部件、Logo 字形/位置/排版及刻度文字；参考场景决定环境。
2. 有目标背景图就以它为场景依据，禁止再添加默认白底颜色建议。2026-09-19 曾因背景参考 + #ffffff 指令冲突生成白底图；原请求确实发送两张图，不得误诊为漏传。
3. 场景已有商品：移除原商品，放入自有商品；没有商品：选择合理承托位置。继承墙地面材质、色彩、布局、光源和投影，不能只模糊地“借鉴风格”。
4. 截图只使用其中有效商品场景，排除手机状态栏、黑边、按钮、价格、原品牌广告和卖点；自有商品本体的文字与 Logo 保留。
5. 不能为仿照参考姿态扭曲商品。融合接触阴影与环境光优先于重设计主体。
6. 多角度必须标明图片角色及编号，只呈现同一件商品；背景和通过样张不能混作商品图。
7. 保存实际 compiled_prompt、图片 ID/哈希、模型、请求 ID、响应使用量和输出原图。结果展示商品、补充角度、目标背景和结果供对照。

## 服务与费用

只用项目显式 .env；禁止输出 key、读取父级凭据或把密钥写入测试/文档。国内 Kimi 与火山方舟白名单；当前精确模型 kimi-k3 和 doubao-seedream-5-0-pro-260628。

真实外发通过预览与绑定请求快照的签名确认；用户在当前会话明确授权重试一次时可按该范围执行。无自动付费重试、无失败后偷偷换模型；批量须首张通过再确认剩余调用次数，幂等防重复收费。不要自行调整账户限额。

Seedream 每次最多10张输入（商品角度、场景、样张合计），一次一张结果；使用2K、b64_json、png。当前 Pro 请求不发送 sequential_image_generation 或 stream；勿未经验证照抄其他版本参数。国内真实联网 Worker 需要网络权限，网络失败不等于 key 错误。

429 必须区分错误码：SetLimitExceeded 是安心体验模式推理限额，余额足也会触发；RateLimitExceeded 系列是频率/吞吐；QuotaExceeded 有多种配额含义；ServerOverloaded 服务繁忙。用户关闭安心体验模式后曾成功返回200。只按安全白名单存错误码，不泄露原始响应中的凭据或URL；不要因 message 出现 model 就称参数错误。早期记录漏存子码，无法倒推。超时未知不能冒充失败未计费。

## UI 与验证

默认显示工具首页，先选择宣传图物料或说明书工业草图。商品、场景独立上传区，支持文件选择/拖入/粘贴；素材可删。参考图有缩略预览，结果在旁边，历史折叠。生成前区分同一商品多角度与多个商品；不能悄悄把多个商品融为一件。

无需账号登录本地服务。删除素材只隐藏，保留历史追溯。不要删除旧任务、改变用户原图或冒充已通过人工质量检查。

修改后运行 .venv/bin/pytest -q、.venv/bin/ruff check .、node --check src/studio/web/static/app.js，并检查浏览器布局与关键输入语义。测试禁真实网络。付费生成只有明确授权才做。重启前检查 queued/running，不中断活跃生成。默认服务127.0.0.1:8765，独立 studio.worker。

尚待增强：按商品分组的多角度批量、可视化主体位置/占比控制、生成前蒙版人工修正、独立商品保真检查。它们不是已完成功能。

确认预览回归检查：展开“查看发送内容”和勾选授权不能使签名预览失效；仅实际生成字段变化可使预览失效。表单 input 冒泡必须按控件类型、字段白名单与预览区域隔离。生成入口固定于右下方的独立底部操作栏（用户指定）。必须按实际栏高预留页面底部空间及滚动余量，不能遮挡确认区；禁止只给表单中的按钮添加 sticky 而无布局避让。

2026-09-19 UI 回归根因：body.dataset.tab 用于样式，但通用 [data-tab] 选择器误把 body 绑定成导航按钮，任何点击冒泡都会 setTab → invalidate。导航事件和 active 状态必须限定 .tabs button[data-tab]，禁止使用全局 [data-tab]。

## 2026-09-20 用户验证：说明书草图

用户确认多角度实拍生成实物图和 A_views 线稿可用。A_views 面向“说明书草图（三视图）· Beta”，用于说明书外观插图初稿、部件说明底图、设计沟通草图。文案强调核对实物后补充标注，不声称完整说明书、精确机械制图或生产加工图。保持已验证的生成提示词不变；本次仅优化功能名称、用途说明与结果指引。

## 2026-09-20 Stitch UI 实现

按用户提供的 Stitch 项目 191993054540350440 实现左资产栏、右分步卡片。随后根据用户指定的新版“极简精工版”更新为黑白浅灰主色、纯黑按钮、少量翡翠绿状态反馈。参见 docs/STITCH_UI.md。原型仅为设计依据，不能照搬虚拟余额、未支持参数或虚构引擎状态。设为主图必须影响真实图片顺序；保持请求预览、确认和批量边界。所有样式在本地，禁止为了原型引入不必要的外部脚本或放宽 CSP。

Stitch 原型还原必须按真实路线显示操作提示：Kimi 只分析输出文字；Seedream 生成图片；白底图及说明书草图不显示无效背景选择；多角度与多商品批量的说明不能混用。界面不得把“要求保留特征”写成已保证像素不变。

## 2026-09-20 连续创作与卡片选择

商品解析可生成白底实物图，结果区“用这张图 → 继续场景混图”将候选 asset_id 设为唯一商品主图，清除旧场景、替换主体及旧补充要求，保留保真要求和比例。只准备表单，不自动收费；生成候选被选用时必须在素材栏可见。实拍仍可直接混图，白底净化不是必经步骤。

商品处理方式、单品多角度/多个商品、画布比例、执行方式与快捷补充采用卡片按钮，实际值仍由原 select 保存，复用既有 change 处理及请求签名失效逻辑。原参考重构 C 路线保留，不能直接改名为已支持的宣传生成链路。

结果新增本地宣传排版：上标题、下说明、深色标题三种基础版式。标题和卖点由用户填写，Canvas 按原图像素尺寸绘制，在额外留白区域加字，不重绘、缩放或裁切商品；因此导出比例随文字区域增加而改变。下载合并 PNG，无模型费用，暂不保存编辑草稿或输出分层模板。不要声称任意广告模板复刻或已经实现完整设计编辑器。

2026-09-20：生成操作栏采用 fixed 独立层级，桌面与右工作区对齐、窄屏通栏。ResizeObserver 按实际高度（包括文字换行与安全区域）更新页面留白和滚动余量。预览、签名确认及批量审批仍沿用原流程；固定栏不绕过确认直接收费。

快捷补充归属 Step 3，与补充要求输入框相邻；点击将预设文字追加到可编辑输入框，保留已有文字，不自动调用模型。

素材选择默认按 fixture 标记过滤测试数据，不能仅靠备注前缀判断。2026-09-20 按用户要求将现存 Fixture 素材软删除，历史引用保留；场景选择仅列出用户上传素材，排除模型原始输出、候选图和抠图结果；生成作品保留在历史里。

## 可选 AI 对照检查（CHECK）

真实生成结果可手动发起 Kimi 检查，先展示图片、原生成要求、接收方及一次调用费用边界，再签名确认。服务器从指定候选及原任务构造不可替换的对照快照：原商品、多角度、替换主体、场景、批量样张、结果分别标注角色。最多10张、24MB；超限明确拒绝，不省略图片。独立任务复用 Worker、签名、幂等、失败不重试及用量追溯。未开展真实模型检查效果验证。

结构化报告区分发现问题、未见明显问题、无法判断，附位置、证据图片及修改建议；不能标成人工通过。三视图不认证尺寸或工程准确性。检查报告保存在 analyses 表但与商品分析隔离，不能关联为生成用商品资料。报告可带入原生成表单的修改意见，用户检查后重新预览；不自动重绘、不自动批准批量、不覆盖人工评价。Fixture 不提供检查入口。

参考场景上传须在上传区就地显示进行中、成功及失败原因（含15MB限制），不能仅在页顶报错。校验失败也须清空文件选择器，让再次选择同一文件触发 change；失败保留原商品与场景。

真实生成/抠图结果提供“加入待处理图片列表”：追加选中该结果，不切换路线、不覆盖已有选择或要求；重复添加不重复、不改变顺序，最多10张。


## 2026-09-20 最新产品契约：两条快速生产流程（覆盖旧版入口约定）

首页只提供两个工具，按工具选择进入工作区：
1. 宣传图物料：同一商品实拍 / 多角度 → A_white 纯净白底展示图 → 可下载或本地抠图；可接续 B2，指定参考场景后生成自己的场景宣传物料。B2 不再显示抠图入口。可使用已有白底图进入场景阶段，不强制重复付费净化。
2. 说明书工业草图：同一商品三视图 / 多角度实拍 → A_views 纯净三视图线稿 → 下载、人工核对后用于说明书外观插图。沿用已验证提示词，不承诺工程准确性。

资产改为随时展开 / 收起的左侧抽屉，支持文件选择、拖放、粘贴；工作区呈现本次选中缩略图。保持固定底部预览按钮、逐次确认、批量首张门槛和历史追溯。场景阶段必须选择参考图片；切阶段清空上一步生成要求，避免白底要求污染参考场景。

逆推提示词、C 配方重构、纸样、测试演示、宣传文字排版、AI 检查等不再作为当前生产工具入口或结果操作。旧后端能力 / 旧记录保留以兼容历史，不扩大本轮功能。未来功能按工具目录扩展，禁止默认把所有能力堆回同一页面。

两工具版验证：67项后端测试与4项前端测试通过；浏览器检查首页资产抽屉、白底预览、白底结果接续B2、A_views预览、历史混图无抠图入口。测试未新增收费生成。前端回归命令：node --test tests/frontend/*.cjs。

## 2026-09-20 最新 Stitch 玻璃工作台

设计源为项目 191993054540350440 的页面 8164f1c1c99f42748bf64b2628feb491（极简玻璃态电商与模特生成工作台），已读取导出 HTML 与截图。沿用玻璃透明度、模糊、渐变、阴影，以及顶部胶囊导航、左侧窄工具栏、左配置右图片流程、下方作品库的构图；代码转为本地 CSS / Jinja / 原生 JS，不能引入原型 CDN、远程示例图、虚拟余额或未接入的模特功能。

首页仍是两工具选择。宣传图右侧展示真实商品 → 白底阶段说明 / 参考场景 → 真实结果；工业草图展示主视图 → 补充角度 → 草图。未生成时显示空状态，不使用示例图冒充结果。本地作品仅显示真实且非 Fixture 的 A_white / A_views / B2 候选，点击回填对应请求与结果；重复打开同一作品也须恢复结果预览。图片角色随实际请求变化，不能把旧场景或旧结果带入新表单。

左侧上传区支持点选、拖入、粘贴和键盘触发，继续复用既有 10 张 / 15MB 校验。资产抽屉保持独立。画布生成按钮与右下固定按钮共用同一预览、确认、禁用状态，不增加调用链或自动收费。保留白底抠图与接续场景、混图批量首张确认。生成结果对照折叠收纳，完整输入快照仍可查看。

浏览器已核对桌面无横向溢出、真实作品回填、白底接续场景、工业草图预览、确认详情展开与勾选不失效。未新增收费生成。

品牌显示约定：左上角仅保留文字 `Storm Studio.`，不显示图标、副标题或品牌徽标。

## 2026-09-20 图片卡即输入区（覆盖此前导航与固定操作栏约定）

用户要求再次精简：顶部居中只显示当前页标题，品牌、页面标题和页脚统一为 Storm Studio.；移除胶囊工具导航、左侧工具轨及右上图片资产按钮。资产入口贴在页面最左边缘，点击展开抽屉。生成仅保留画布内一个按钮，取消固定底部栏。

白底页仅有“自己的商品 → 白底展示图”两张卡，箭头说明去除杂物与背景。点击输入卡弹出本地上传、作品库选择与已选图片管理，也支持向卡片拖图、聚焦后粘贴。多图展示首张为主的堆叠和总数，不误示为单张。混图保留商品、参考场景、成图三张卡。补充要求与保留特征折叠在流程下方，画布比例直接可见。

工业草图分为一张主视图、最多五张补充角度、生成草图。主视图替换不能覆盖其余角度，删除主视图后不得悄悄把角度提升为主图；必须重新选主图后才能提交。上传、作品选择及提交均校验数量，实际请求仍以主图在前的 product_ids 发送给原有生成链路。

结果卡显示准备预览、等待确认、任务排队、生成中及终态。加载不伪装成已创建付费任务；签名确认保持不变。失败、未知或中断后停止加载，不自动重试。真实结果与原始快照继续保留。当前离线验证为67项后端及7项前端测试；浏览器检查白底两卡、多图堆叠、工业草图三张输入预览、混图参考选择和确认区不意外收起。未做新增付费生成。

## 2026-09-20 白底图收敛为单张实拍（覆盖此前白底多角度约定）

用户反馈三张不同角度生成白底图时出现结构拼接，偏离净化实拍的目标。A_white 仅允许一张商品实拍，暂不提供辅助角度入口。新选图替换当前白底输入；历史多图请求仍可查看，但不能原样再次提交，必须明确重新选择一张。前后端同时拒绝多张商品图及额外参考、背面、场景、主体、蒙版图片，不能只靠前端隐藏。

白底提示词明确保持原观察视角、透视、朝向、轮廓比例、支撑和展开/折叠状态，只移除背景、杂物与外围广告；不拼接正侧背面、不补画不可见结构、不添加面板、支架或商品副本。这是生成约束，不能宣称已保证像素级保真。本次修改未新增收费生图，视觉改善待真实验证。A_views 的 1+5 多角度以及 B2 的已验证场景链路保持原有语义与提示词。

单张输入替换、多图拒绝、隐藏背面排除和后端旁路校验已纳入回归；68项后端与8项前端测试通过。

## 2026-09-20 批量首张确认必须紧邻画布

本次真实故障：批量请求正确保存两件商品，供应商按契约只生成首件；画布却只读取 product_ids（当次送模的一件），忽略 batch_product_ids，导致其余商品看似消失。渲染已有批次时必须优先使用完整 batch_product_ids，含剩余子任务；不能将批量图片误改成同商品多角度。

成图下直接显示批次商品缩略图、逐件状态、当前第几件，以及“通过，继续生成余下 N 张”和“不满意，修改后重新生成”。首张完成后暂藏通用生成按钮，避免用户误建重复首张。通过仍使用原有签名预览与确认，剩余任务使用同一参考场景及通过的候选作为 anchor；不自动付费、不变更后端幂等。修改必须恢复完整商品列表、参考场景、批量模式和保留要求，展开补充意见，再生成新首张。取消批量预览应保留样张与决策按钮。

剩余任务按 anchor_candidate_id 精确归属到通过样张，展示排队、执行、完成、失败或未知状态；已全部提交后不再显示继续提交与修改首张按钮。历史样张也可进入此流程。浏览器复用用户已有两商品任务验证完整列表、余下1张预览含商品/场景/样张3张输入及不满意修改保留2个商品；未新增付费生成。68项后端与10项前端测试通过。

## 2026-09-20 统一结果卡，取消重复结果大栏

工作区只保留流程右侧一处结果呈现。排队、执行、失败、未知和完成状态归属该结果卡；完成后在卡片下直接下载 PNG、加入待处理、白底接续混图和白底抠图。批量决策仍在三张流程卡下方，只显示一次。新的生成、历史作品和本地抠图均使用同一结果入口，不能出图后又跳到页面底部另一个“生成结果”大栏。

旧结果容器移动到默认关闭的“作品详情”原生对话框（在生成表单之外，避免评价表单嵌套）。记录、原始输入对照、处理记录、人工评价、收藏、ZIP追溯导出保留。旧分析 / 配方记录仍可通过详情查看。失败原因在结果卡内可见，重试需要恢复设置后重新确认，不自动重试。打开详情不改变生成输入；从详情继续创作时关闭对话框。

浏览器验证真实批量作品只显示一处成图与批量决策、详情包含评价与请求记录、白底可继续混图、历史作品回到对应工作区。没有新增付费生成。

场景混图允许无素材直接进入。路线切换不校验是否已有商品图，不强制先走白底生成；用户可以先切换再上传已有白底图或实拍与参考场景。商品和场景完整性仍在生成预览 / 提交时校验。

场景混图的商品卡和选图弹窗提示优先使用清晰白底商品图，以帮助保留细节、提升混图效果；仍允许实拍，不承诺必然保真或强制白底。

历史任务列表仅在独立历史作品页显示；首页及生产工作区底部不再展示重复的历史任务栏。右上历史作品入口与历史恢复功能保留。

## 2026-09-20 新任务与历史恢复分离

从首页进入任一工具始终创建空白任务，不沿用上次恢复的历史配置。工作区右上提供“新建任务”，保留当前功能阶段（白底、场景混图或工业草图），清空本次商品、补充角度、参考场景、补充要求、保留特征、生成结果及尚未提交的确认信息；比例恢复 1:1，商品处理方式恢复默认。历史作品只有在用户明确点击恢复时才回填。

新建不删除资产、历史记录或已有结果，也不取消已提交的生成任务。任务切换使用草稿版本隔离，历史读取、轮询、预览与上传等旧异步响应不得重新填入新任务。浏览器已验证恢复三商品混图后经首页进入白底为空白，以及在混图内新建仍留在混图、资产和作品保留。11 项前端及 68 项后端测试通过；未新增付费生成。

## 2026-09-20 常用画布比例

白底、场景混图和工业草图共用九种画布比例：1:1、3:4、4:3、4:5、5:4、2:3、3:2、9:16、16:9。前端按钮、请求校验和 Worker 导出尺寸保持一致，尺寸定义集中在 domain/canvas.py。保持原有 2K 模型请求，将所选比例写入提示词；结果仍等比适配并补边，不拉伸或裁切商品，不声称模型原生严格遵守比例。9:16 导出为 1440×2560，旧比例导出尺寸不变。

九种比例均完成 Fixture 与模拟供应商的离线 Worker 验证，检查提示词、保存记录及输出文件实际宽高比。87 项后端、11 项前端测试通过；浏览器确认 9:16 可选择、布局无横向溢出。未新增收费生图。

## v0.1 Beta 发布

本轮两条生产链路由用户确认完成，发布标签为 `v0.1.0-beta.1`，Python 包版本为 `0.1.0b1`。版本功能与限制见 CHANGELOG.md。仅提交代码、测试、锁文件及文档，不提交本地凭据、运行数据、模型权重或临时产物；提交不添加工具署名。
